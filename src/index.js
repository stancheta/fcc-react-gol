import React from 'react';
import ReactDOM from 'react-dom';
import GolDashboard from './GolDashboard/GolDashboard';
import './index.sass';

ReactDOM.render(
  <GolDashboard />,
  document.getElementById('root')
);
